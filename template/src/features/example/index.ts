// ============================================================================
// FEATURE DE EXEMPLO
//
// Esta pasta (`src/features/example`) existe apenas para demonstrar a estrutura
// e as convencoes do template (model / services / components / tests + index
// como interface publica). Ela DEVE SER REMOVIDA ou RENOMEADA durante o setup
// do seu projeto real.
//
// Regras respeitadas aqui e que valem para toda feature:
// - Outras features NAO importam arquivos internos desta (so este index.ts).
// - A interface publica da feature e exposta exclusivamente por este arquivo.
// ============================================================================

export { ExampleCounter } from './components/ExampleCounter';
