// ============================================================================
// FEATURE DE EXEMPLO
//
// Esta pasta (`src/features/example`) existe apenas para demonstrar a estrutura
// e as convenções do template (model / services / components / tests + index
// como interface pública). Ela DEVE SER REMOVIDA ou RENOMEADA durante o setup
// do seu projeto real.
//
// Regras respeitadas aqui e que valem para toda feature:
// - Outras features NÃO importam arquivos internos desta (só este index.ts).
// - A interface pública da feature é exposta exclusivamente por este arquivo.
// ============================================================================

export { ExampleCounter } from './components/ExampleCounter';
